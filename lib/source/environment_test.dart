
/// Defines the environment configuration for the application test.
/// This includes the base URL for API endpoints test.
class EnvironmentTest {
  final String url;

  /// Constructs an Environment object with the given URL.
  EnvironmentTest({required this.url});

  /// Creates an environment configuration for the development environment.

  /// Creates an environment configuration for the production environment.
   String printData() {
    return "Hello ";
  }
}
